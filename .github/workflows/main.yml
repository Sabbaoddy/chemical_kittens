on:
  push:
    branches:
    - main
    
jobs:
  build:
    runs-on: ubuntu-latest
  - name: Validate YAML
    uses: brittonhayes/validate-yaml
    with:
      schemaPath: 'schema.json'
      files: |
        example/*.yaml
